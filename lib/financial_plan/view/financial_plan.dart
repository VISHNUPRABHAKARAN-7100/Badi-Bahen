import 'package:flutter/material.dart';

class FinancialPlan extends StatelessWidget {
  const FinancialPlan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Financial Plan'),
    );
  }
}
